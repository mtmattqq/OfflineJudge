// 以下請新增 include 項目
#include<bits/stdc++.h>
using namespace std;

int main() {
    // code
    int a, b;
    cin >> a >> b;
    cout << a + b;
    chrono::milliseconds s(100);
    this_thread::sleep_for(s);
}